name: voiceguard-processor

channels:
  - defaults
  - conda-forge  
  - https://repo.anaconda.com/pkgs/main
  - https://repo.anaconda.com/pkgs/r
  - https://repo.anaconda.com/pkgs/msys2

dependencies:
  - python=3.12.9
  - ffmpeg=6.1.1

  - pip:
      - pydub==0.25.1
      - requests
      - tqdm

prefix: /home/username/miniconda3

